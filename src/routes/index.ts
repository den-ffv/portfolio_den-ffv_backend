import { Router } from "express";
import userRoutes from "./userRoutes";

const router: Router = Router();

// router.use('/user', userRoutes)
// router.use('/routPath', routeImportName)
// router.use('/routPath', routeImportName)

export default router