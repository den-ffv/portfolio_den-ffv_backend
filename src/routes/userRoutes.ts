import { Router } from "express";

const userRoutes: Router = Router();


// userRoutes.post("/routPath", nameController.nameMiddleware);
// userRoutes.get("/routPath", nameController.nameMiddleware);
// userRoutes.path("/routPath", nameController.nameMiddleware);
// userRoutes.delete("/routPath", nameController.nameMiddleware);


export default userRoutes